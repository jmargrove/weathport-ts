import * as React from "react";
import mainLogo from "../assets/logo.svg";

const Logo = () => {
  return <img src={mainLogo} style={{ width: "70px", marginTop: "15px" }} />;
};

export default Logo;
